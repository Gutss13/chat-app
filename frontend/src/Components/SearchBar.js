import axios from 'axios';

function SearchBar(props) {
  // const handleChange = (e) => {
  //   axios
  //     .get(`http://localhost:3000/people/${localStorage.id}/${e.target.value}`)
  //     .then((request) => {
  //       return request.data;
  //     })
  //     .then((data) => {
  //       props.setFriends(data);
  //     });
  // };

  return (
    <div>
      {/* <input
        type="search"
        onChange={(e) => {
          handleChange(e);
        }}
      /> */}
      Need to add search for friend list here
    </div>
  );
}

export default SearchBar;
